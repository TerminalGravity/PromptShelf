import Foundation
import SwiftUI

// This file is used to help resolve ambiguous type references
// by providing type aliases to the existing types

// Empty file - no type redefinitions needed
// All Swift files in the same module can see each other's types without imports 